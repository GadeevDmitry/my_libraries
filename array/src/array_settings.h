#ifndef ARRAY_SETTINGS_H
#define ARRAY_SETTINGS_H

//#define ARRAY_NVERIFY
//#define ARRAY_NDEBUG

//--------------------------------------------------------------------------------------------------------------------------------

#if defined(NVERIFY)
#define ARRAY_NVERIFY
#endif

#if defined(NDEBUG)
#define ARRAY_NDEBUG
#endif

#endif // ARRAY_SETTINGS_H
